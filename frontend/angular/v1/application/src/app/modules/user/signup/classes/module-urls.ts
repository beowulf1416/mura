import { Urls } from '../../../../classes/urls';

export class ModuleUrls {
    public static url_user_register = Urls.base_url + 'v1/user/register';
    public static url_user_verify = Urls.base_url + 'v1/user/verify';
}
